name: lint
on: [push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
      - run: go vet -json ./... > diagnostics.json
      - name: annotate diagnostics
        uses: kyoh86/go-check-action@latest
        with:
          level: error
